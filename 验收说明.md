# v1.1验收说明 · 2026-09-05

结论：发现并修复了v1.0多个真实放行漏洞，同时优化长期记忆保存和上下文恢复。当前仍是未开书的通用工作流，没有代表用户确认题材或试写品质。

## 实际复现的旧版问题
在v1.0原包的临时副本中，以下5个操作均未被原检查器拦截：审稿证据任意填写、审稿后改变参考结论、审稿后改变人物记忆、删除最后一个归档目录、替换初始授权记录。相关路径已纳入新版回归。

## 验证范围与结果
- 50项自动回归通过。覆盖完整归档/接续、正文/记忆/参考/主线/文风反馈使旧审稿失效、证据不存在、评分底线、P1阻塞、跨集合ID、世界事实对象悬空、道具持有者、知识获得途径、伏笔窗口、规则日期、JSON重复键、布尔值冒充整数、末章误删、前驱链损坏、授权记录篡改、复制期间变更、并发锁、事务恢复、恢复候选损坏、分章账本、检索更新、分页完整性和关联线索选择。
- 60章合成数据完成顺序归档与完整校验，共60个事件只存60次；同样每章一事件的旧设计将存1830条重复快照记录。第1章事件可通过检索找回。
- 当前环境60章完整校验约0.059秒；这是短测试文本在当前机器的单次结果，不能外推真实百万字小说的速度。
- 正式交付目录状态：未初始化、0章、无活跃草稿、无残留写锁。

复现命令：
```powershell
python -m unittest discover -s tests -v
python tests/scale_check.py
python tools/novel.py status
```

## 未被测试证明的事项
测试中的“通过/100分”都是数据夹具，不能证明文学水平。没有进行真实用户题材的开篇试写、百万字语义连续性测试、Windows实机测试、投稿平台验收或恶意攻击防护验证。
当前实体状态仍采用快照；实体/知识量增长仍会增加存储与校验成本。SQLite是中文子串检索副本，不是语义召回系统。全量历史校验会随章数变慢。
LOCKED的追加/变更和已有旧稿迁移仍需基于具体书稿的版本化处理，不提供会覆盖旧档的一键操作。断电级耐久与不同机器的并发编辑不在保证范围内。
